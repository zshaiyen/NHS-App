use vkyoxfwenk_nhsapp;

/*
DROP TABLE app_user;
CREATE TABLE app_user (
    app_user_id int NOT NULL AUTO_INCREMENT,
    full_name varchar(250),
    photo_url varchar(500),
    email varchar(500),
    PRIMARY KEY (app_user_id)
);
*/

-- insert into app_user (full_name, photo_url, email) values('Sarmad', 'none', 'sarmad@gmail.com');
-- insert into app_user (full_name, photo_url, email) values('Zane', 'none', 'zaneshaiyen@gmail.com');
select * from app_user;
